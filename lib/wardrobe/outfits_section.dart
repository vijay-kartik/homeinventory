import 'package:flutter/material.dart';

class OutfitsSection extends StatelessWidget {
  const OutfitsSection({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Outfits Section'),
    );
  }
} 